<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/brilliantBlue</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowSoftInputMode">stateAlwaysHidden</item>
        <item name="colorControlActivated">@color/brilliantBlue</item>
    </style>

    <style name="ActivityNoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="buttonStyle">
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">6dp</item>
        <item name="android:background">@drawable/styled_button_background</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/app_button_text_size</item>
        <item name="android:fontFamily">@font/proximanova_regular</item>
    </style>

    <style name="editTextStyle">
        <item name="android:padding">8dp</item>
    </style>
    
    <style name="TextAppearance.AppCompat.Widget.ActionBar.Title" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">25dp</item>
        <item name="android:fontFamily">ProximaNova-Regular</item>
    </style>

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:fontFamily">@font/proximanova_regular</item>
    </style>

</resources>
